({
  optimize: "none",
  appDir: "built", // relative to this file
  dir: "built", // just build into the same directory
  baseUrl: "./", // relative to appDir above (app)
  modules: [
    { name: "main"},
  ]
})

